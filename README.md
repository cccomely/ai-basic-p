###开发目标：
很显然，如果希望得到数据文件，那么网页端的AI是不能完成任务的，必须开发相应的AI agent，但考虑到对数据文件格式暂没有特别的要求，所以选了相对常见的csv格式。
###推送说明：
1月7日首次运行aiagent.py并未取得成功，纵然已在命令行下生成了result.csv。原因是AI的回复并不是每次都带有一个一模一样的'### 说明：'，所以修正以后加入了以'|'标志来判断是否数据已全部给出。然后得到了newresult.csv。
1月13日，对首尾两个'|'符号的提取逻辑都顺利调整完毕，至于reslai1[0]为何会包含数据表格的标题行，仍有待考证。生成的temecv2.csv没有保存上传。
1月23日，对首行的提取逻辑进行了调整。。
2月12日，测试了ssh登陆后的远程开发，以及在本地退出登陆甚至本地系统重启后的ssh连接状态，同时增加了txt文件品种。
2月22日，费了老鼻子劲，完成了aiagentpdf.py文件的编写，主要是在一个pdf里集成多个表格是相当陌生且有一定难度的，完了还调整了一下aiagentcsv.py的写入内容，并新增了若干pdf和一个csv文件。
2月25日，调整了pdf智能体中baizohi参数带来的逻辑，修改了pdf和xlsx智能体中的提示，但主要目的是完成了将字体修改为ttf文件，向SourceHan系列字体文件的开发者致敬！
4月2日，本地增设.ssh/config文件，使得本地用单独的密钥文件连接成为可能。
5月31日，新增了智能体文件aiagentcsvpy.py，对于原来的aiagentcsv.py，实在无法忍受其中的大量ifelse代码，于是统统精简，新文件变得更加pythonic了，在千问指导下，也首次尝试了一次性双变量条件赋值使用海象运算符的写法，但不清楚同样是简化编写它为啥不被推荐使用。
6月23日，在码云gitee上增加一个备份，形成CNB、github之后第三个云上仓库。
